{version, {shell, "echo -n `git describe --tags --abbrev=0`"}}.

{profiles, [
    {default, [
        {prepare, [clean]}
    ]}
]}.

{cc, [
    {name, jiffy},
    {sources, ["c_src", "c_src/double-conversion"]},
    {linux, [
        {ldflags, "-lstdc++"}
    ]}
]}.
